# Default configuration for FloridaBESTtoPDF

# Base URL template with placeholders for grade and page.
# {grade} will be replaced by DEFAULT_GRADE.
# {page:04d} will be replaced by the zero-padded page number.
BASE_URL_TEMPLATE=https://bim.easyaccessmaterials.com/programs/fl2023/{grade}/page{page:04d}.xhtml

# Default grade level (e.g., gradek, grade1, grade2)
DEFAULT_GRADE=grade3

# Default page range settings
START_PAGE=31
# COUNT=772
LAST_PAGE=848

# Processing options
CLEAR_OUTPUT_AT_START=true  # Delete all files in output directories before starting
ENABLE_WHITE_BLACK=true
ENABLE_PDF=true
ENABLE_COLOR_PDF=true       # Generate color PDF from raw images
ENABLE_ONE_PDF=true

# PDF naming
PDF_NAME=grade2_book        # Base name for PDF files (without extension)

# Default output settings
OUTPUT_DIR_RAW=output/imgs_raw
OUTPUT_DIR_PROCESSED=output/imgs_processed
OUTPUT_DIR_PDF=output/pdfs
IMG_FORMAT=png
